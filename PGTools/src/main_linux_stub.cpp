#include <iostream>

auto main() -> int
{
    std::cout << "pgtools Linux build is currently a compatibility stub.\n";
    std::cout << "Full patching functionality is available on Windows builds.\n";
    return 0;
}
